Just like the VS code agent sessions run GLM 5.2 from the Bring Your Own Key service in the same way I want to run the VS code chats on web in such a way that I have my VM and on the VM deploy the VS code web and on that VS code web it will be password protected the password will be known to me and it will be in env hash renien Uh so using that V address I will map it to some domain the area code to some domain and using its IP address when I will call that IP at port 80 maybe after / we escort then this VS code will open up and over there I can add my own NVIDIA build API keys to run my own custom models like GLM 5.2 NVIDIA Nemotron models Kimi K2 .6 models and other deep seek etc models through API calls on build NVIDIA this is the architecture I want woodland right now it is working on my laptop perfectly or it is too slow and I want to switch off my laptop so that work can autonomously continue the VM since VM will always be online so how to do it I mean make it happen. will use gcloud vm with lest cost as long as possible and deploy VS code web on it. 